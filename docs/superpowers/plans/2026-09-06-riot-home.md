# Riot Home cards implementation plan

Approved design: five Home player cards show Riot icon/ID, Solo/Duo tier/division/LP and ranked wins/losses, plus three champions from up to 50 recent Solo/Duo matches, timestamp and refresh. NA1 platform / Americas routing. Preserve roster/pools/manual results/reference archive.

- [x] Backend Riot client: fixed HTTPS hosts, token header only, timeouts, serialized throttling, sanitized errors; account → summoner + ranked + queue420 match IDs/details. Aggregate only matching PUUID and queue420, skip early-surrender remakes, deterministic top three; label actual sample.
- [x] Cache successful snapshots in a new V5 table keyed by normalized saved Riot ID. Read-only GET serves cache; POST starts bounded background refresh; polling reads progress without additional Riot calls. Failed refresh retains previous snapshot. Never expose PUUID or key in frontend payload.
- [x] Home card component with separate rank and recent sample, fallback avatar, empty/error/loading states, manual refresh and polling. Preserve existing roster controls.
- [x] Aggregation, transport errors, cache identity and card tests; full suite 24 backend / 18 frontend passed. Final name mapping passed 5 focused backend tests and frontend lint/build. Live Hai Legacy sync succeeded (48 eligible of 50 fetched); verified icon/rank/champions/timestamp visually and cache persistence after restart. Independent review found two completion races, now corrected. No commits.

Usage and API documentation: `docs/riot-home.md`. Other four players remain unsynced until Refresh is selected; no automatic external requests on Home load.
