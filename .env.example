# Copy to .env and choose a local password. Do not commit .env.
POSTGRES_DB=beatrice
POSTGRES_USER=beatrice
POSTGRES_PASSWORD=replace-with-your-local-password
POSTGRES_PORT=5432

# Backend only. Never prefix this with VITE_ or commit your real key.
RIOT_API_KEY=
